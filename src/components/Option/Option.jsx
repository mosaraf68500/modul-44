
// import PropTypes from 'prop-types';
// import propTypes from 'prop-types'
const Option = ({Option}) => {
    const { name}=Option
    return (
        <div>
        <h1>{name}</h1>
            
        </div>
    );
};

// Option.propTypes={
//     Option:PropTypes.object
// }

export default Option;